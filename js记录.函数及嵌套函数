已经开始实习了，关于node.js 和 mongodb的。为了方便实习，我自学了js。
想说一下关于函数的问题：
在别的语言里面我们定义一个函数：
function f()
{
var x =10;
x = x+1;
return x;
}
在js里面函数可以视为一个物件（object）,所以我们可以将函数定义为：
var f = function(){ //我们称之为匿名函数
var x =10;
x = x+1;
return x;
} 
其中f 的属性为function （typeof f "function"）
我理解为一个函数当它只是被定义并没有被执行的时候，在f后面不会跟（），同时这个物件的类型为function。
当我们需要这个函数被执行时，我们需要调用内存生成一个空间，然后在空间里面建立一个变量var x.
然后var x = window.f();
当函数被执行的时候就要带上括号。还有windows一般情况下可以省略， 函数变为var x = f(); 在这里x 的类型是number 因为放回的值
是11。其实var x 也是可以省略的， 我们可以直接执行f(),执行这个相当于每次系统建立一个新的对象; //typeof f() "number" 
如果没有定义函数，则f（）的类型为undefined

下面说一下嵌套函数，这个东西有两个作用，一个是在外部访问内部的参数，另一个是保留参数，因为嵌套函数执行在他的定义环境
举个例子：
var  f =()=>{var x =10;return ()=>{x=x+1;return x}}
var x = f()
x()
11
x()
12
f()()= > var y = f(); y();
11
f()()=> var z =f();z();
11
这里要说一下，因为第一次return的是一个方程，所有x的属性就是方程，方程式可以执行的所以有x（）
可以看出var x =10 这个属性已经存在x()这个方程里面了，如果var x1= x（）；那么x1就是一个实例化的对象，里面的值将不再发生变化。
